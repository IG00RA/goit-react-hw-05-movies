// import { getProducts } from '../../API';

const Movies = () => {
  // const movies = getProducts();
  return (
    <main>
      <input placeholder="Please enter film name" />
      <button type="button">Search</button>
      {/* <ProductList products={movies} /> */}
    </main>
  );
};

export default Movies;
